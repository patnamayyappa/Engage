import { NetworkVisDirective } from './network-vis.directive';

describe('NetworkVisDirective', () => {
  it('should create an instance', () => {
    const directive = new NetworkVisDirective();
    expect(directive).toBeTruthy();
  });
});
